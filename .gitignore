# .NET build artifacts
bin/
obj/
*.user
*.suo
.vs/

# Publish output and assembled bundle (rebuilt via build/build.sh)
build/out/
build/AvrdudeUI.app/

# macOS
.DS_Store

# JetBrains
.idea/

# Persisted user config (should never leak into the repo)
config.xml
presets.xml

# Icon build intermediate — regenerated by build/icon/make-icns.sh
build/icon/AvrdudeUI.iconset/

# Local test firmware files
*.hex
*.eep
